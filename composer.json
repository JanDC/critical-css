{
    "name": "jandc/critical-css-processor",
    "description": "Twig postprocessor class which uses the the page-specific-css library to inject the document's head with css",
    "type": "library",
    "require": {
        "jandc/twig-wrapper-provider": "^0.2",
        "jandc/page-specific-css": "^0.2"
    },
    "license": "MIT",
    "authors": [
        {
            "name": "Jan Decavele",
            "email": "jandecavele@gmail.com"
        }
    ],
    "autoload": {
        "psr-4": {
            "CriticalCssProcessor\\": "src/"
        }
    }
}
